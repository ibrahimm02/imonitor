iMonitor - Cloud Monitoring as a Service

To setup and run the app:

- create virtual enviroment
  python -m venv venv

- activate virtual environment
  source venv/bin/activate

- install requirements
  pip install -r requirments.txt

- run the app
  flask run
